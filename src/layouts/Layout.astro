---
const { title = "PopOff", description = "Step into the Spotlight" } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Import Poppins from Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">

    <style>
      /* Apply Poppins font sitewide */
      body {
        font-family: 'Poppins', sans-serif;
        font-weight: 600; /* Bold for everything by default */
        margin: 0;
        padding: 0;
      }

      /* Headlines even bolder */
      h1, h2, h3, h4, h5, h6 {
        font-weight: 800; /* Extra bold for headlines */
      }
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
